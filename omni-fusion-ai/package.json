{
  "name": "omni-fusion-ai",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "description": "A one-key multi-model AI workspace where agents visibly hand work to one another through resilient step-by-step collaboration.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test tests/*.test.js",
    "check": "node --check api/team-step.js && node --check api/orchestrate.js && node --check api/status.js && node --check api/image.js && node --check api/speech.js && node --check api/providers/runtime.js && node --check api/providers/openrouter.js && node --check api/providers/catalog.js && node --check api/lib/routing.js && node --check app.js"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
