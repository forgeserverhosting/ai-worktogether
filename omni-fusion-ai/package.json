{
  "name": "omni-fusion-ai",
  "version": "10.0.0",
  "private": true,
  "type": "module",
  "description": "A compact website-first multi-AI studio with in-chat questions, visible collaboration, multimodal picture analysis, real files, previews, validation, and ZIP downloads.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test tests/*.test.js",
    "check": "node --check api/orchestrate.js && node --check api/team-step.js && node --check api/project-step.js && node --check api/package-project.js && node --check api/status.js && node --check api/image.js && node --check api/speech.js && node --check server/providers/runtime.js && node --check server/providers/openrouter.js && node --check server/providers/catalog.js && node --check server/lib/routing.js && node --check app.js"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
